SECRET_KEY=your-secret-key-here
DEBUG=True

# Option A: Full DATABASE_URL (used by Render, Railway, Supabase, etc.)
DATABASE_URL=postgresql://user:password@host:5432/campaign_tracker

# Option B: Individual DB settings (used when running locally)
DB_NAME=campaign_tracker
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# NewsAPI key — get a free key at https://newsapi.org/
NEWS_API_KEY=your_newsapi_key_here
